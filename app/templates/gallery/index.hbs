<div class="container mx-auto">
  <Token::Grid @tokens={{shuffle this.model}} />
</div>
{{#let (or this.web3.hasGlobalEth (not (media 'isXl'))) as |hideToast|}}
  {{#unless hideToast}}
    <Metamask-Toast />
  {{/unless}}
{{/let}}